{% extends 'base.html.twig' %}

{% block title %}SubCategory{% endblock %}

{% block body %}
<div class="container">
        <h1>Voir Sous Catgorie</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ sub_category.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ sub_category.nom }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_sub_category_index') }}">back to list</a>

        <a href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}">Modifier</a>

        {{ include('sub_category/_delete_form.html.twig') }}
</div>
{% endblock %}
